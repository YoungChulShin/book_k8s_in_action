apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mongodb-pvc
spec:
    resources:
        requests:
            storage: 1Gi   # 요청 용량
    accessModes:
        - ReadWriteOnce
    storageClassName: ""